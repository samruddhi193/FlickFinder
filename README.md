# FlickFinder
A data mining and warehousing capstone that builds a movie recommendation engine using collaborative filtering (UBCF, IBCF, SVD), content-based filtering, association rule mining, and a hybrid model — backed by a star-schema SQLite warehouse on the MovieLens 25M dataset.
